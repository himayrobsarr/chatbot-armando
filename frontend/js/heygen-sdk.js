import StreamingAvatar, { AvatarQuality, StreamingEvents, TaskType } from '@heygen/streaming-avatar';

window.HeyGenSDK = {
    StreamingAvatar,
    AvatarQuality,
    StreamingEvents,
    TaskType
};